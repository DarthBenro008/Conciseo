<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDarkCardBackground</item>
        <item name="colorPrimaryDark">@color/colorRisingText</item>
        <item name="colorAccent">@color/colorFallingText</item>
        <item name="font">@font/rubikmedium</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/colorDarkCardBackground</item>
    </style>

</resources>
